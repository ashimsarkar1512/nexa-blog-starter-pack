import Spinner from "@/components/ui/Spinner";

const loading = () => {
            return (
                        <div>
                        <Spinner></Spinner>            
                        </div>
            );
};

export default loading;